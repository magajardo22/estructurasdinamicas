from clases import *
from funciones import *

def main():
    playlist = lista()
    cola_m = Cola()
    pila_m = Pila()
    crear_lista(playlist)
    #imprimir(playlist)
    #buscar_index(playlist,2)
    #buscar(playlist,"Creep")
    #eliminar_lista(playlist, "Creep")
    #print(playlist.tamanio)
    #crear_cola(playlist, cola_m)
    #imprimir_cola(cola_m)
    #estimar_tiempo(cola_m,15,2)
    #crear_pila(playlist,pila_m)
    #imprimir_p(pila_m)
    #raiz = crear_arbol(playlist)
    #dif_buscar(playlist,raiz,"Creep")
  

if __name__ == "__main__":
    main()